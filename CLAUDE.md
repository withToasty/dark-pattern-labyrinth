# CLAUDE.md

このリポジトリは複数プロジェクトを含む。Claude Code は、ユーザーが指定した対象だけを扱う。

## 基本ルール

- 作業開始時にリポジトリ全体を読み直さない。
- 指定されたIssue / PR / ユーザー指示に必要なファイルだけ読む。
- 無関係なフォルダや既存プロジェクトを変更しない。
- `Tk-work` は、明示的な許可がない限り読み込み・編集・移動・削除・追加をしない。
- 大規模リファクタリングは、明示的に求められない限り行わない。
- 既存の公開インターフェースや出力形式は、必要なく変更しない。
- 同じ検証を無意味に繰り返さない。
- 外部モデルや大容量データは、複数候補を大量取得せず、まず有力な1つで検証する。
- main への merge は、ユーザーが明示的に依頼した場合のみ行う。
- 作業終了時の報告は短くする: Changed / Verified / Blocked / Next。

## 作業指示の正本

個別タスクは GitHub Issue を正本とする。
ユーザーからIssue番号を指定された場合、そのIssue本文と対象PRを読んで作業する。

リポジトリ内にタスク管理用の `TASK.md` や `STATUS.md` は置かない。
Claude自身が進捗管理のためのMarkdownファイルを追加・更新しない。

必要な経緯はIssue / PR / git historyから、今回の作業に必要な範囲だけ確認する。

## コンテキスト節約

- 過去チャットを再構築しない。
- repo-wide reviewをしない。
- Issueで指定された範囲を優先する。
- 実装に不要な長い設計文書を作らない。
