# CLAUDE.md

このリポジトリは複数プロジェクトを含む。Claude Code は、ユーザーが指定した対象だけを扱う。

## 基本ルール

- 作業開始時にリポジトリ全体を読み直さない。
- 指定されたプロジェクト・タスクに必要なファイルだけ読む。
- 無関係なフォルダや既存プロジェクトを変更しない。
- `Tk-work` は、明示的な許可がない限り読み込み・編集・移動・削除・追加をしない。
- 大規模リファクタリングは、明示的に求められない限り行わない。
- 既存の公開インターフェースや出力形式は、必要なく変更しない。
- 同じ検証を無意味に繰り返さない。
- 外部モデルや大容量データは、複数候補を大量取得せず、まず有力な1つで検証する。
- main への merge は、ユーザーが明示的に依頼した場合のみ行う。
- 作業終了時の報告は短くする: Changed / Verified / Blocked / Next。

## city-techno-vision の作業

`city-techno-vision/` を扱う場合は、最初に次の2ファイルだけ確認する。

1. `city-techno-vision/STATUS.md`
2. `city-techno-vision/TASK.md`

`STATUS.md` = 現在地・確定事項・未解決事項。
`TASK.md` = 今回だけ実行するタスク。

必要になった場合のみ、README・コード・PR・その他のファイルを読む。

`city-techno-vision/CLAUDE_FENCE_TASK.md` は過去の詳細タスク記録。現在の作業では `TASK.md` を正本とし、特別な理由がない限り読み込まない。

## コンテキスト節約

チャット履歴より GitHub 上の `STATUS.md` / `TASK.md` を優先する。
過去の経緯を再構築するために大量のコミットや会話を読むのではなく、必要な差分だけ確認する。
