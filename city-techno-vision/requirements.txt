ultralytics>=8.0.0
opencv-python>=4.8.0
PyYAML>=6.0
# needed only for open-vocabulary detection (YOLO-World models' set_classes()),
# see README "open-vocabulary detection"
clip @ git+https://github.com/openai/CLIP.git
# needed only for fence detection (--fence-model / FenceDetector), see
# README "fence detection" and src/fence_detector.py
torch
torchvision
transformers
pillow
# needed only for the web UI/API (web/app.py), see README "Web UI"
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
